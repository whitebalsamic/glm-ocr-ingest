"""Project-level defaults for the local GLM-OCR wrapper."""

# Developers can change this without editing the main CLI.
# If this value is removed or left empty, the CLI falls back to CPU.
DEFAULT_LAYOUT_DEVICE = "cpu"
